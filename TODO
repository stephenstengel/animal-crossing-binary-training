TODO:
	Add command line parameter to give a base folder to the output folders so that they can be segmented by experiment settings.
	
	Add math for finding steps per epoch; not sure if it's automatic.

	Pass nearly all global values into functions through main

	Get image size from text file. Or just look at the image.
	
	Add more models.
	
	Add a free licence.

	Make examples of error images
	
	Save weights to a file.
	
	Load our weights from a file.
	
	
